@model IEnumerable<IcoItem>
@{
    ViewBag.Title = "Ico Index";
}

<h2>Top ICOS</h2>


@*@foreach (var item in Model)
{
<div class="row card-panel">

    <div class="col col-6 col-md-3">
        <i class="fa fa-star" style="color: blue;"> @item.ListPosition.PositionValue </i>
        <a asp-route-id="@item.Id" asp-controller="IcosList" asp-action="IcoDetail">
            @item.Name (@item.Symbol)
        </a>
    </div>
    <div class="col col-5 col-md-2">
        @item.TotalTokenCount.ToString("##,###") @item.Symbol
    </div>
    <div class="col col-5 col-md-2">
        @item.Status
    </div>

    <div class="col col-md-3">
        <i class="fa fa-clock-o" style="color: green; font-size: 20px;"></i>
        @item.StartTime.ToString("dd.MM.yyyy") - @item.EndTime.ToString("dd.MM.yyyy")
    </div>
</div>
}*@
<table class="striped">
    <thead>
        <tr>
            <th>#</th>
            <th>Name</th>
            <th>Token Count</th>
            <th>Status</th>
            <th class="text-center"><i class="fa fa-clock-o" style="color: green; font-size: 20px;"></i></th>
        </tr>
    </thead>
    @foreach (var item in Model)
    {
    <tr>
        <td> @item.ListPosition.PositionValue</td>
        <td>
            <a asp-route-id="@item.Id" asp-controller="IcosList" asp-action="IcoDetail">
                @item.Name (@item.Symbol)
            </a>
        </td>
        <td>@item.TotalTokenCount.ToString("##,###") @item.Symbol</td>
        <td>@item.Status</td>
        <td class="text-center">@item.StartTime.ToString("dd.MM.yyyy") - @item.EndTime.ToString("dd.MM.yyyy")</td>
    </tr>
    }

</table>

@*<div class="row">
        <table class="striped">
            <thead>
            <tr>
                <th>Name</th>
                <th>Symblo</th>
                <th>Start Time</th>
                <th>End Time</th>
            </tr>
            </thead>
            <tbody>
            @foreach (var item in Model)
            {
                <tr>
                    <td><a href="#">@item.Name</a></td>
                    <td>@item.Symbol</td>
                    <td>@item.StartTime</td>
                    <td>@item.EndTime</td>
                </tr>
            }
            </tbody>
        </table>
    </div>*@


